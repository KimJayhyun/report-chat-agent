[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KimJayhyun", email = "dnlwjem000@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk[http-server]>=1.1.2",
    "langchain>=1.3.14",
    "langgraph>=1.2.10",
    "uvicorn>=0.52.1",
]

[project.scripts]
agent = "agent:main"

[build-system]
requires = ["uv_build>=0.8.0,<0.9"]
build-backend = "uv_build"
